import { DestroyComponentDirective } from './destroy-component.directive';

describe('DestroyComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DestroyComponentDirective();
    expect(directive).toBeTruthy();
  });
});
