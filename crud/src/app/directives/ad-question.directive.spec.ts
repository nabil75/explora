import { AdQuestionDirective } from './ad-question.directive';

describe('AdQuestionDirective', () => {
  it('should create an instance', () => {
    const directive = new AdQuestionDirective();
    expect(directive).toBeTruthy();
  });
});
